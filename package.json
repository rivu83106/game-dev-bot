{
  "name": "game-dev-bot",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register": "npx tsx scripts/register.ts guild",
    "register:global": "npx tsx scripts/register.ts global",
    "db:init": "wrangler d1 execute game-dev-bot-db --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.12",
    "tsx": "^4.19.1"
  }
}
